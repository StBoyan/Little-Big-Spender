<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/item_transfer_account"
        android:icon="@drawable/ic_transfer"
        app:showAsAction="always"/>

    <item
        android:id="@+id/item_add_account"
        android:title="@string/accounts_addAccount"
        app:showAsAction="never"/>

    <item
        android:id="@+id/item_add_crypto"
        android:title="@string/accounts_addCrypto"
        app:showAsAction="never"/>

</menu>
